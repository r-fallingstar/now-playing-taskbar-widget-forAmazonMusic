<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>WinExe</OutputType>
    <TargetFramework>net8.0-windows10.0.19041.0</TargetFramework>
    <UseWPF>true</UseWPF>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <RootNamespace>SpotifyTaskbarWidget</RootNamespace>
    <AssemblyName>AmazonMusicTaskbarWidget</AssemblyName>
    <ApplicationManifest>app.manifest</ApplicationManifest>
    <ApplicationIcon>app.ico</ApplicationIcon>
    <SatelliteResourceLanguages>en</SatelliteResourceLanguages>
    <Version>1.0.0</Version>
    <Product>Now Playing - Taskbar Widget for Amazon Music (Unofficial)</Product>
    <AssemblyTitle>Now Playing - Taskbar Widget for Amazon Music (Unofficial)</AssemblyTitle>
    <Company>r-fallingstar (fork based on MechanicWB)</Company>
    <!-- Um único .exe no publish, para as atualizações substituírem só um ficheiro -->
    <RuntimeIdentifier>win-x64</RuntimeIdentifier>
    <PublishSingleFile>true</PublishSingleFile>
    <SelfContained>false</SelfContained>
  </PropertyGroup>

</Project>
